---
apiVersion: v1
kind: Service
metadata:
  name: service-front
  namespace: 
spec:
  selector:
    app: frontend
  ports:
    - name: port-80
      port: 80
      protocol: TCP
      targetPort: 80